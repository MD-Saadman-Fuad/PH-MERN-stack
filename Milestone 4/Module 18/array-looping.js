const fruits = ['apple', 'anar', 'watemelon', 'daab'];

for(let i = 0; i<fruits.length;i++){
    console.log(fruits[i]); 
}

let k = 0;

while (k<fruits.length){
    console.log(fruits[k]);
    k++;
}